<div id="book-<%= book.id %>">
  <div class="book book-<%= book.id %>">
    <div class="book-shape">
      <h2 class="title"><%= book.title %></h2>
      <p>By</p>
      <p><%= book.author %></p>
    </div>
  </div>
</div>
